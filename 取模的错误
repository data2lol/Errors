很多题目要求对求得的结果取模，之前犯了一个错误
比如：
for i (1..10){
  sum += dfs(i);
  sum%=11911;
}
这样的取模是错误的，正解应该是：
for i (1..10){
  sum += dfs(i)%11911;
}

根据取模的公式看，假设sum = a1 + a2 + a3; 那么 sum%f = a1%f + a2%f + a3%f;
按照上面第一种写法， 其实是 ((a1%f + a2)%f + a3)%f; 所以没有悬念的错啦。。。hao蠢
